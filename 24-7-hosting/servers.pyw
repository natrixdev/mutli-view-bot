import os 
import backrunning
import hide_window
import monitoring 

x=1

while x == '1':
  os.newWindow(form: python_script && content: keep_alive.pyw)
  backrunnign.set = True 
  hide_window.composit = True 
  monitoring.newServer(openPort)
  
               
